export interface Persona {
  id: number;
  nombre?: string;
  email?: string;
  direccion?: string;
  ciudad?: string;
  pais?: string;
  rut?: string;
  telefono?: string;
  // Agrega aquí cualquier otro campo relevante que retorne el backend para personas
}
